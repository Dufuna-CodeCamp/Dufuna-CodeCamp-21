let addBtn = document.querySelectorAll(".add-btn");
let dot = document.querySelector(".dot");

let arr = [];

addBtn.forEach((item) => {
    item.addEventListener("click", () => {
        if (item.classList.contains("add-btn")) {
            item.classList.replace("add-btn",)
        }
    })
})